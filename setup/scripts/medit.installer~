#!/usr/bin/env sh



sudo apt-get install -y slibxtst-dev git gcc make pkg-config libx11-dev libxtst-dev libxi-dev
	
git clone https://github.com/alols/xcape.git 
cd xcape
make
sudo make install
cd ..
rm -rf xcape